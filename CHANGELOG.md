# Changelog

## Unreleased

- **Breaking:** Lead enrichment retired. `Lead`, `LeadBatch`, `GetLeadBatch`, `WaitForLeadBatch` and their `Lead*` request/response types are removed. Use `Extract` with your own schema for structured page extraction.
